#include <stdio.h>
#include <stdlib.h>
void main(){
    int a[10],b[10],n,i;
    printf("\n enter size of both the arrays:");
    scanf("%d",&n);
    printf("\n enter array elements of a and b:");
    for(i=0;i<n;i++){
        scanf("%d %d",&a[i],&b[i]);
    }
    
}